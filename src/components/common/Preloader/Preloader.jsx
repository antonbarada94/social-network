import React from 'react'
import preloader from '../../../img/Rolling-1.2s-157px.svg'

const Preloader = (props) => {
   return (
      <div>
         <img src = {preloader} />
      </div>
   );
}

export default Preloader;